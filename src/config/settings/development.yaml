# 开发环境配置

# 继承base.yaml
app:
  debug: true

# 日志配置
logging:
  level: "DEBUG"
  console: true

# LLM配置
llm:
  provider: "deepseek"
  temperature: 0.7
  
# 缓存配置
cache:
  type: "memory"

# 分析配置
analysis:
  save_intermediate_results: true
  log_tool_outputs: true